#def num(list):
   # nums = []
    #for i in list:
       # if i.is_integer():
            #nums.append(i*2)
       # else:
           # nums.append(i*4)
    #print(nums)
#num([2,2.5,4,6.5,8])


#def naames(names):

   # converted_names = []
    #for i in range(len(names)):
    # if i % 2 == 0:
          # converted_names.append(names[i].upper())
    # else:
       #  converted_names.append(names[i].lower())

   # print(converted_names)
#naames(["saba", "nika", "vano", "giorgi", "lazare", "dachi"])

#def capitalize_sentence(sentence):
    #words = sentence.split()
   # for i in range(len(words)):
        #words[i] = words[i].capitalize()
    #capitalized_sentence = ' '.join(words)
    #return capitalized_sentence
#print(capitalize_sentence("this sentence contains alot for words for a code im gonna test"))