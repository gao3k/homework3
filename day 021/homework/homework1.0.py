#def manual_pop(lst, index=None):
    #if index is None:
       # index = len(lst) - 1
    
   # if index < 0 or index >= len(lst):
        #raise IndexError("Index out of range")
    
   # return lst[:index] + lst[index+1:]

# Example usage:
#my_list = [1, 2, 3, 4, 5]

# Removing the element at index 2
#result = manual_pop(my_list, 2)
#print("After removing element at index 2:", result)

# Removing the last element (default behavior)
#result = manual_pop(my_list)
#print("After removing last element:", result)
